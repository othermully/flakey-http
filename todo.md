# Todo list instead of a bunch of comments in code
- HTTP requests always end in newline char
- Error handling
- Work around blocking states, poll, fork, etc idk yet
- JSON interface
- std::function for route creation? 
- send JS and CSS when requested 
